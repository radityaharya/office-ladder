# Postgres connection string (Supabase project "office-ladder")
# Project Settings > Database > Connection string > URI (use the pooler connection for serverless-y hosts, direct for a VPS)
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.vdhumwwdgwuhtyurijtp.supabase.co:5432/postgres"

# openssl rand -base64 32
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"
